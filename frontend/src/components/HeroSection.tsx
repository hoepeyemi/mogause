'use client';

import React from 'react';

// Re-exported from page.tsx placeholder — this was the old HeroSection.
// In v2 the hero is embedded directly in the page header. This file
// exists so any stale import won't break the build.

export default function HeroSection() {
  return null;
}
